{
  "name": "frontend-ui",
  "version": "1.0.0",
  "source": "src/index.html",
  "scripts": {
    "dev": "parcel src/*.html",
    "build": "parcel build src/*.html --dist-dir ../public --no-source-maps --public-url ./"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.16.3",
    "@parcel/optimizer-htmlnano": "^2.16.3",
    "parcel": "^2.16.3"
  },
  "browserslist": "> 0.5%, last 2 versions, not dead"
}